# 2026 年广东省大学生工程实践与创新能力大赛
## 智能物流搬运赛项 · 备赛指南 V2

> **说明**：本文档严格依据赛题原文提炼要点，不增删比赛规则内容。各板块末尾提供备赛建议，以实际应用与可复用设计为导向。
> ⚠️ **加粗警示项**为硬性要求，违反即扣分或取消资格。
> 🆕 **V2 新增**：决赛颜色识别增强策略（LAB / HSV+RGB / k-NN 等多方案）；决赛双码任务调度器设计（拾取码 + 放置码分离架构）。

---

## 目录

1. [参赛作品要求](#一参赛作品要求)
2. [赛程安排](#二赛程安排)
3. [运行环境要求](#三运行环境要求)
   - [（二补）决赛颜色识别增强策略](#二补决赛颜色识别增强策略)
   - [（四补）决赛双码任务调度器设计](#四补决赛双码任务调度器设计)
4. [赛项具体要求](#四赛项具体要求)
5. [快速备赛 Checklist](#附快速备赛-checklist)

---

## 一、参赛作品要求

### （一）功能要求

#### 核心要点

| 功能 | 要求描述 | 重要级别 |
|------|----------|----------|
| 自主运行 | **机器人必须完全自主运行，全程无人干预** | ⚠️ 硬性 |
| 定位与移动 | 自主导航，识别并沿车道行驶 | 必须 |
| 避障 | 动态规避场地内障碍 | 必须 |
| 读取二维码 | 识别 80×80mm 亚光二维码，获取任务编码 | ⚠️ 硬性 |
| 颜色识别 | 识别红、绿、蓝三色物料及其在转盘上的位置 | ⚠️ 硬性 |
| 物料抓取与载运 | 每次最多搬运 3 个；**物料须承载于机器人上，不允许夹持运送** | ⚠️ 硬性 |

#### 💡 备赛建议

1. **模块化软件架构**：将二维码识别、颜色识别、路径规划、机械臂控制各封装为独立模块，单独标定、单独测试，后期换平台可直接移植复用。
2. **颜色识别鲁棒性**：初赛推荐 HSV 色彩空间做阈值分割，比 RGB 更不受补光影响；**决赛若新增颜色种类，建议升级为多色彩空间融合方案**（见三（二补）决赛颜色识别增强策略），将所有颜色参数写入配置文件，赛前现场微调无需重新编译。
3. **转盘抓取策略**：转盘 6–10s/圈、每次停 4s，建议实现"等停再抓"逻辑（检测到停止信号后再执行抓取），避免对运动目标抓取引入位置误差。
4. **承载平台设计**：机器人顶面需保证物料在运动中不滑落，可设计定位槽或限位挡柱，槽位尺寸依据物料 φ50mm 直径设计，该方案可在初赛/决赛间复用。

---

### （二）电控及驱动要求

#### 核心要点

- 传感器和电机的种类及数量不限
- **⚠️ 必须配备任务码显示装置**：字体高度 ≥ 8mm，位于机器人上部醒目位置，亮光显示，且不被任何物体遮挡
- **⚠️ 各机构只能使用电驱动**，不允许气动、液压等
- **⚠️ 采用锂电池供电**（规格不限），随车装载，**比赛过程中不能更换**
- **⚠️ 初赛和决赛过程中，不能通过任何其它交互手段与机器人通信及控制**
- **⚠️ 比赛过程中仅允许垂直向下进行补光**，不允许对场地进行遮挡

#### 💡 备赛建议

1. **显示装置不可忽视**：建议使用 OLED 或 LED 数码管，字高留余量（≥10mm）；安装后需从裁判视角四向检查无遮挡死角——这是高频扣分项。
2. **电池容量规划**：按完整运行时间×最大功率计算，预留 1.5 倍余量；因比赛中途不能换电，一旦电量不足失败无法补救，建议赛前满电出发并记录续航时间。
3. **合规补光方案**：补光灯垂直朝下固定在机器人上，随机器人移动，属合法使用；禁止使用平行照向场地的补光灯，否则判违规。
4. **离板自主运行**：发车后禁止一切外部干预，所有参数须在启动前配置完毕，程序不依赖赛时 WiFi/蓝牙指令。

---

### （三）机械结构要求

#### 核心要点

- **⚠️ 机械部分必须参赛选手自主设计并制造**
- **⚠️ 除标准件外，非标零件应自主设计和制作**
- **⚠️ 不允许使用购买的成品或采用成品套件拼装**
- **⚠️ 决赛时须在创新实践环节按指定要求完成零件设计与制作**

#### 💡 备赛建议

1. **手爪针对性设计**：初赛物料夹持部为球体（φ50mm），建议设计内凹弧面夹爪，增大接触面积，抓取稳定性显著优于平面夹爪；此方案在决赛多形状物料场景下也有较好兼容性。
2. **参数化 CAD 模板**：提前在 SolidWorks/Fusion360 中建立参数化模型，关键尺寸用变量驱动，决赛创新实践时仅需修改参数即可出图，大幅节省时间。
3. **文档与实物一致**：裁判可能核对设计文档，零件名称、材料、工艺描述须与实物吻合，修改实物后文档必须同步更新。
4. **折叠机构可靠性**：若采用可折叠设计，展开机构需多次测试确保稳定，建议加设机械限位防止过展导致结构损坏。

---

### （四）外形尺寸要求

#### 核心要点

| 维度 | 限制 | 重要级别 |
|------|------|----------|
| 铅垂方向投影（含机械手臂） | **不大于 300mm × 300mm 正方形** | ⚠️ 硬性 |
| 高度 | **不超过 400mm** | ⚠️ 硬性 |
| 折叠结构 | 允许可折叠，但**出发之后**才可自行展开 | 条件允许 |
| 违规后果 | **不满足要求不能参赛；参赛后发现取消成绩** | ⚠️ 硬性 |

#### 💡 备赛建议

1. **制作验证工具**：制作 300×300mm 检验框和 400mm 限高杆，作为日常调试的标准量具，每次改动结构后必检。
2. **CAD 铅垂投影校验**：机械臂展开后的投影是最易超标区域，在 CAD 软件中提前做铅垂方向投影分析，勿仅凭目测判断。
3. **展开可靠性测试**：折叠展开机构应在不同电量下均能可靠触发，建议设置展开完成的检测反馈（如限位开关），防止未完全展开就启动任务。

---

## 二、赛程安排

### 赛程结构

| 环节 | 阶段 | 内容 | 关键事项 |
|------|------|------|----------|
| 第一环节 | 初赛 | 设计文档评审 | 正确性 + 创新性 |
| 第二环节 | 初赛 | 现场初赛 | **现场发布决赛任务命题** |
| 第三环节 | 决赛 | 现场实践（制造零件） | **未完成取消资格** |
| 第四环节 | 决赛 | 现场决赛 | 按决赛命题完成物料运输 |

#### 核心要点

- 初赛取排名**前 50% 左右**进入决赛
- **⚠️ 初赛成绩不带入决赛**，决赛完全重置
- 每队每阶段有**两次运行机会**，取最好成绩
- 初赛总成绩相同时：依次按现场初赛成绩→完成时间排序

#### 💡 备赛建议

1. **设计文档是第一关**：需提前完成并反复打磨，正确性（计算、选材、电路）与创新性同等重要，是进决赛的必要条件。
2. **决赛机会均等**：初赛成绩不带入决赛，即使初赛排名靠后进入决赛后仍有逆转机会，决赛备赛不可松懈。
3. **创新实践紧接决赛**：现场制造零件后须立即安装调试，时间极为紧张，建议提前规划好加工流程和调试顺序，并演练全流程。
4. **两次机会分配策略**：第一次稳健完成基础任务保底，第二次尝试更高效路线或更多物料一次性搬运，争取更高分数。

---

## 三、运行环境要求

### （一）比赛场地

#### 场地参数

| 项目 | 参数 | 备注 |
|------|------|------|
| 赛场尺寸 | 2400mm × 2400mm 正方形平面 | — |
| 车道宽度 | 400mm 和 450mm 两种 | 底色为**灰色**，机器人只能在车道上行驶 |
| 非车道区域 | 亚光白色或黄色等底色 | 不可行驶区域 |
| 启停区（蓝色） | 300 × 300mm | 机器人出发与回归点 |
| 原料区（转盘） | 顶面直径 300mm，总高度 80–100mm | 转盘中心距启停区边界约 1600mm |
| 粗加工区 / 暂存区 | 580 × 150mm | 顶面有色环评分标记 |
| 二维码板 | A4 横放，二维码 80×80mm 亚光 | **板中心距启停区边界约 800mm** |

#### 转盘参数

- 转盘匀速转动速度：**6–10 秒/圈**
- 每圈停留次数：**3 次**，每次停留 **4 秒**
- 物料分两批放置，每批 3 个，中线呈 **120° 夹角**放置

#### 💡 备赛建议

1. **1:1 复现赛场**：尤其是灰色车道与白/黄底色区域的颜色差异，确保机器人巡线逻辑在实际颜色下稳定工作，不可只在实验室地板上测试。
2. **等停抓取逻辑**：按最快转速（6s/圈）设计等待时间；停留时间仅 4s，手爪需在 2s 内完成定位→抓取→回收动作，需重点优化抓取速度。
3. **二维码识别距离**：测试时要覆盖低电量/低帧率情况下的识别稳定性，确保在 60–120cm 范围内均能可靠解码，不依赖最优距离。
4. **动态定位而非硬编码**：场地摆放位置在允许范围内变化，代码中不可硬编码坐标，需依赖传感器实时定位（激光雷达/视觉/IMU 均可），提高对位置变化的鲁棒性。

---

### （二）搬运物料

#### 初赛物料

| 属性 | 规格 |
|------|------|
| 包络尺寸 | 直径 φ50mm、高度 70mm 圆柱体 |
| 重量 | 约 50g |
| 夹持部形状 | **球体** |
| 材料 | 3D 打印 ABS |
| 颜色 | 红（C-21-03）、绿（C-21-06）、蓝（C-21-04）|
| 数量 | 每种颜色 2 个，共 6 个；每批红/绿/蓝各 1 个，共 2 批 |

#### 决赛物料

| 属性 | 规格 |
|------|------|
| 颜色/材料 | 与初赛相同 |
| 形状 | 简单几何体及组合体（圆柱、方形、三角、球、锥、组合体等）|
| 尺寸 | 各边长/高度/直径限制在 **30–70mm** |
| 重量 | **40–80g** |

#### 💡 备赛建议

1. **精准颜色标定**：依据题目 ABS 颜色编号打印实物样品，在比赛场景的补光条件下采集颜色数据，精确标定 HSV 阈值；**决赛可能新增颜色**，初赛使用单一色彩空间须确保扩展性，详细升级方案见下方"决赛颜色识别增强策略"小节。
2. **通用手爪设计**：决赛物料形状多样，推荐柔性或三点接触自适应夹持方案，兼容多种几何形状，比针对球体优化的手爪鲁棒性更强，且可在初赛与决赛间复用。
3. **夹持力设计余量**：重量上限 80g，手爪夹持力按 80g × 1.5 = 120g 保持力设计，确保在加速/减速过程中不掉落。

---

### 🆕（二补）决赛颜色识别增强策略

> **决赛风险点**：决赛物料颜色可能与初赛相同（红/绿/蓝），但不排除新增颜色种类的可能。建议在初赛颜色识别方案基础上提前完成升级，做到"新增颜色只需 3 分钟重标定"，无需修改任何代码。

#### 方案一：LAB 色彩空间（推荐决赛主力方案）

**原理**：CIE LAB 是感知均匀色彩空间，L 通道（亮度）与 A（红绿轴）、B（蓝黄轴）通道分离。在不同光照下颜色距离的变化远小于 RGB 或 HSV，是赛场光照条件存在不确定性时的最优选择。

| 对比维度 | RGB | HSV | LAB |
|----------|-----|-----|-----|
| 补光变化敏感度 | 高（三通道同时变化） | 中（V 通道受影响） | **低**（L 通道独立，可归一化）|
| 新颜色标定难度 | 中 | 中 | **低**（欧氏距离直觉性强）|
| 相似颜色区分度 | 低 | 中 | **高**（感知均匀，距离即差异）|
| 代码复杂度 | 低 | 低 | 低（OpenCV 一行转换）|

**实现要点**：

```python
import cv2, json, numpy as np

# 转换色彩空间
lab = cv2.cvtColor(frame, cv2.COLOR_BGR2LAB)
a_ch, b_ch = lab[:, :, 1], lab[:, :, 2]

# 标定：对每种颜色采集 ROI 样本，保存均值与标准差
def calibrate_color(roi_lab, color_name, config):
    a_vals = roi_lab[:, :, 1].flatten()
    b_vals = roi_lab[:, :, 2].flatten()
    config[color_name] = {
        "a_mean": float(np.mean(a_vals)), "a_std": float(np.std(a_vals)),
        "b_mean": float(np.mean(b_vals)), "b_std": float(np.std(b_vals))
    }
    with open("colors_config.json", "w") as f:
        json.dump(config, f, indent=2)

# 分类：计算马氏距离（或简化为加权欧氏距离）
def classify_color(a, b, config):
    best, min_dist = "unknown", float("inf")
    for name, params in config.items():
        dist = ((a - params["a_mean"]) / (params["a_std"] + 1e-6)) ** 2 \
             + ((b - params["b_mean"]) / (params["b_std"] + 1e-6)) ** 2
        if dist < min_dist:
            min_dist, best = dist, name
    return best if min_dist < CONFIDENCE_THRESH else "unknown"
```

**标定流程**（可复用的 3 步法）：
1. 将物料放置于赛场补光条件下，运行标定脚本，框选 ROI 区域
2. 脚本自动提取 LAB 均值与标准差，追加写入 `colors_config.json`
3. 重启识别模块，验证识别准确率 ≥ 95% 即可

---

#### 方案二：HSV + RGB 双空间投票融合

**原理**：HSV 擅长处理色调变化，RGB 在稳定光照下绝对精度高；两个分类器独立判断后加权投票，比单一空间对异常情况更鲁棒。

```
分类器 A（HSV）：H 通道阈值范围 + S/V 有效范围过滤 → 输出颜色标签 + 置信度 P_hsv
分类器 B（RGB）：与参考颜色的欧氏距离归一化 → 输出相似度得分 P_rgb

动态权重融合：
  亮度高（光照强）→ w_rgb 上升（RGB 精度更高）
  亮度低（光照弱）→ w_hsv 上升（H 通道更稳定）

final_label = argmax(w_hsv × P_hsv + w_rgb × P_rgb)
```

**适用场景**：赛场光照条件与实验室标定环境差异较大，或物料表面有反光/哑光差异时，效果优于单一方案。

**可复用设计**：两个分类器封装为独立类，通过接口调用，权重可在配置文件中调整，无需改动核心代码。

---

#### 方案三：k-NN 样本匹配（新颜色快速接入）

**原理**：存储每种颜色的若干参考样本点（推荐 LAB 空间），分类时计算待测特征向量与所有样本的欧氏距离，k 个最近邻投票决定类别。

| 对比项 | 阈值法（HSV/LAB）| k-NN |
|--------|-----------------|------|
| 新颜色接入速度 | 需人工设置阈值 | **只需采集 20–50 个样本点追加** |
| 边界颜色处理 | 易误判 | **投票机制更鲁棒** |
| 推理速度 | 极快（< 1ms）| 快（5ms，样本量 < 500 时）|
| 代码量 | 少 | 少（sklearn 3 行实现）|

```python
from sklearn.neighbors import KNeighborsClassifier
import pickle, numpy as np

# 离线训练：保存各颜色 LAB 样本
clf = KNeighborsClassifier(n_neighbors=5, metric='euclidean')
clf.fit(X_train, y_train)          # X: LAB A/B 特征，y: 颜色标签
pickle.dump(clf, open("knn_model.pkl", "wb"))

# 在线推理
clf = pickle.load(open("knn_model.pkl", "rb"))
label = clf.predict([[a_val, b_val]])[0]
prob  = clf.predict_proba([[a_val, b_val]]).max()  # 置信度
if prob < 0.8:
    label = "unknown"              # 低置信度触发重识别
```

**决赛快速接入新颜色**：只需补充新颜色的 LAB 样本点，重新 `fit` 并保存模型，整个过程 < 3 分钟，**不修改任何任务逻辑代码**。

---

#### 方案四：多色彩空间并行投票（最高鲁棒性）

同时运行 LAB、HSV、RGB 三个分类器，三者共同投票（多数决），任意单一分类器误判不影响最终结果。

```
LAB 分类 → 标签 A
HSV 分类 → 标签 B
RGB 分类 → 标签 C
final_label = majority_vote(A, B, C)
三者不一致时 → 输出 "uncertain"，触发重识别
```

计算量略增（树莓派/Jetson Nano 上单帧处理 < 8ms），在高精度要求场景下推荐使用。

---

#### 💡 颜色识别系统工程规范

1. **配置文件驱动**：所有颜色参数（LAB 均值/标准差、HSV 范围、k-NN 模型路径等）统一存储在 `colors_config.json`，新增颜色只更新配置文件，不改动一行代码。
2. **标定工具脚本化**：开发一个 ≤ 60 行的标定脚本，运行后自动采集当前视频帧中 ROI 的颜色特征并追加到配置文件，做到"3 分钟完成一种新颜色标定"。
3. **置信度兜底**：分类结果置信度低于阈值时标记为 `"unknown"`，触发重识别逻辑（最多 3 次），而非强行判断为某种颜色导致抓错物料。
4. **形态学后处理**：颜色分割后加腐蚀+膨胀操作去除噪点，再用连通域分析确认物料位置，面积过小的连通域直接过滤。
5. **赛场二次标定**：进入赛场调试时间段，立即用标定工具在实际补光条件下对所有颜色重新采集一次，更新配置文件，覆盖实验室标定值。

---

### （三）色环评分系统

#### 评分对照表

| 环号 | 外径尺寸（φ = 物料最大直径） | 得分 |
|------|------------------------------|------|
| 1 环（Φ1） | φ + 3 mm | **15 分** |
| 2 环（Φ2） | Φ1 + 5 mm | 10 分 |
| 3 环（Φ3） | Φ2 + 7 mm | 7 分 |
| 4 环（Φ4） | Φ3 + 10 mm | 5 分 |
| 5 环（Φ5） | Φ4 + 10 mm | 3 分 |
| 6 环（Φ6） | Φ5 + 10 mm | 1 分 |
| 6 环外或物料倾倒 | — | **0 分** |

> **评分判定方式**：从色环外界垂直方向是否能看到该圈外圈来判定（外圈被物料遮住 = 落在该环内）。色环线宽为 1.5mm。

**码垛放置**：以是否平稳放置在已有物料上来评分（第二批物料须叠放在第一批上）。

#### 💡 备赛建议

1. **精度目标量化**：1 环仅比物料直径多 3mm，容忍的落点偏差极小。建议将末端定位精度目标设定为 **±1.5mm** 以内，有余量冲击 1 环（15 分），与 6 环（1 分）差距极大，精度直接决定排名。
2. **实物色环测试**：在粗加工区/暂存区顶面打印实物比例色环，进行专项精度测试，不要只依赖空跑轨迹数据；累计统计落环分布，持续优化。
3. **码垛高度自适应**：码垛放置不应使用固定 Z 轴偏移量，建议通过视觉或激光传感器确认下层物料的实际高度后再下放，防止因高度误差导致倾倒（倾倒=0分）。
4. **缓速下放**：放置动作最后 10–20mm 建议降速，减少冲击力，降低物料倾倒风险。

---

### （四）任务编码系统

#### 编码规则（初赛）

| 要素 | 说明 |
|------|------|
| 编码数字含义 | 1 = 红色，2 = 绿色，3 = 蓝色 |
| 初赛格式 | **两组三位数，以"+"连接**，如 `123+231` |
| 第一组 | 第一批 3 个物料：原料区 → 粗加工区的搬运顺序 |
| 第二组 | 第二批 3 个物料：原料区 → 粗加工区的搬运顺序 |
| 同顺序转运 | 粗加工区 → 暂存区的顺序与对应批次从原料区搬运至粗加工区的顺序相同 |
| 编码来源 | 机器人读取赛场二维码获取（编码随机产生）|
| 决赛任务码 | **根据现场发布命题确定，未提前公布** |

#### 💡 备赛建议（初赛）

1. **解析即显示**：二维码解析完成后，立即在任务码显示装置上亮显供裁判核验；解析失败需有重试逻辑（建议最多 3 次），超时后停在原地等待判定，不要盲目继续运行。
2. **统一调度器**：将任务码解析、颜色–位置映射、抓取序列生成封装成统一调度器，输入字符串 `"123+231"`，输出有序动作队列；提前测试所有 `3! × 3! = 36` 种组合，确保无漏洞。
3. **颜色确认前置**：搬运前须对转盘上的物料做颜色确认，避免因随机摆放导致抓错颜色，颜色识别是任务执行的前置条件。
4. **决赛适配性**：决赛任务格式可能与初赛不同，调度器需支持动态更新任务队列，不可将初赛的双组三位格式写死，保持对未知格式的扩展能力——详见下方"决赛双码任务调度器设计"。

---

### 🆕（四补）决赛双码任务调度器设计

> **决赛扩展风险**：决赛任务码可能从初赛的"单码同序"模式演变为**拾取码（PICK）+ 放置码（PLACE）分离**的双码模式，两码独立编码、独立解析，搬运顺序与放置顺序不再相同。以下为头脑风暴推演与系统化应对方案。

---

#### 双码模式的核心区别

| 模式 | 初赛（单码）| 决赛预案（双码）|
|------|------------|----------------|
| 任务码数量 | 1 组（拾取即放置）| 2 组（拾取码 + 放置码各自独立）|
| 拾取顺序 | 由任务码直接决定 | 由拾取码单独决定 |
| 放置顺序 | 与拾取顺序相同 | 由放置码单独决定，可与拾取码完全不同 |
| 调度器复杂度 | 低（顺序直接映射）| 中（需做排列组合映射）|
| 颜色标签解耦 | 非必须 | **必须**（以颜色为中间键联结两码）|

---

#### 可能出现的双码格式

**格式 A：双码合并在同一二维码**

| 格式示例 | 含义 |
|----------|------|
| `P:213\|D:312` | 拾取顺序 G→R→B，放置顺序 B→R→G |
| `123+231/312+213` | 批次1：拾取123，放置312；批次2：拾取231，放置213 |
| `PICK=213&PLACE=312` | URL 参数风格 |
| `{"pick":"213","place":"312"}` | JSON 结构化 |

**格式 B：双二维码，两次独立扫码**

- 码板 A（路径前段，距启停区 ~800mm）：拾取任务码，读取后前往原料区
- 码板 B（路径中段，位置待赛前公布）：放置任务码，拾取完成后前往读取，再执行放置

应对：状态机新增"读放置码"状态，在完成拾取、前往放置区之间插入扫码步骤。

---

#### 双码调度核心逻辑：以颜色为中间键

初赛逻辑中"拾取顺序 = 放置顺序"是隐式假设，双码模式下需显式打破这一假设，**以颜色标签为中间键**联结两码：

```
【初赛逻辑——拾取即放置，顺序直接映射】
拾取码 123 → 依次抓取 R, G, B → 依次放置在位1(R), 位2(G), 位3(B)
机器人槽位分配：槽1=R, 槽2=G, 槽3=B（与放置目标一一对应，无需重排）

【双码逻辑——拾取与放置解耦，颜色为中间键】
拾取码 213 → 依次抓取 G, R, B
           → 机器人内部槽位状态：{槽1: G, 槽2: R, 槽3: B}
放置码 312 → 目标位映射：{位1: B, 位2: R, 位3: G}
调度器动作：
   位1需要B → 查映射表 B 在槽3 → 从槽3取出 → 放置到位1
   位2需要R → 查映射表 R 在槽2 → 从槽2取出 → 放置到位2
   位3需要G → 查映射表 G 在槽1 → 从槽1取出 → 放置到位3
```

---

#### 任务调度器代码框架（可复用）

```python
# color_map: 编码数字 → 颜色标签
COLOR_MAP = {"1": "RED", "2": "GREEN", "3": "BLUE",
             "4": "YELLOW", "5": "WHITE"}  # 预留决赛新颜色

class TaskScheduler:
    def __init__(self):
        self.color_to_slot = {}    # 颜色标签 → 机器人槽位（拾取后建立）
        self.pickup_queue  = []    # [(颜色标签, 转盘位置), ...]
        self.place_queue   = []    # [(目标放置位, 来源槽位, 颜色标签), ...]
        self.task_code_str = ""    # 原始任务码，用于显示装置

    # ── 解析层（插件式，新格式只加一个函数）──────────────────────
    def parse(self, raw_code: str, fmt: str = "auto"):
        """支持多种格式，由配置文件指定 fmt"""
        if fmt == "auto":
            fmt = self._detect_format(raw_code)
        pick_str, place_str = PARSERS[fmt](raw_code)
        self._build_pickup(pick_str)
        self._build_place(place_str)
        self.task_code_str = raw_code

    def _detect_format(self, code: str) -> str:
        if "|" in code:  return "pipe"       # "P:213|D:312"
        if "/" in code:  return "slash"      # "123+231/312+213"
        if "+" in code:  return "plus"       # 初赛格式 "123+231"
        return "single"                       # 单码兜底

    # ── 建立拾取队列与槽位映射 ────────────────────────────────────
    def _build_pickup(self, pick_str: str):
        for slot_idx, ch in enumerate(pick_str):
            color = COLOR_MAP[ch]
            self.color_to_slot[color] = slot_idx + 1
            self.pickup_queue.append(color)

    # ── 建立放置队列（双码模式核心） ─────────────────────────────
    def _build_place(self, place_str: str):
        for pos_idx, ch in enumerate(place_str):
            color = COLOR_MAP[ch]
            slot  = self.color_to_slot.get(color)
            if slot is None:
                raise ValueError(f"放置码要求颜色 {color}，但拾取码中不含此颜色")
            self.place_queue.append({
                "target_pos": pos_idx + 1,
                "source_slot": slot,
                "color": color
            })

    # ── 执行接口（供机器人控制层调用）────────────────────────────
    def execute_pickup(self, robot):
        for color in self.pickup_queue:
            robot.move_to_turntable()
            robot.wait_for_color(color)          # 等停后识别颜色再抓取
            robot.pickup(color)
            robot.load_to_slot(self.color_to_slot[color])

    def execute_place(self, robot):
        for item in self.place_queue:
            robot.take_from_slot(item["source_slot"])
            robot.move_to_position(item["target_pos"])
            robot.place_with_precision()         # 缓速下放，视觉微调

    def get_display_str(self) -> str:
        """返回任务码显示装置内容"""
        return self.task_code_str
```

---

#### 状态机扩展：双码运行流程

```
┌─────────────────────────────────────────────────────────────┐
│                    双码模式状态机                            │
├─────────────────────────────────────────────────────────────┤
│  INIT                                                        │
│    ↓ 一键启动                                                │
│  SCAN_QR_PICK（扫拾取码）                                    │
│    ↓ 解析成功 → 显示拾取码                                   │
│  [若双二维码模式] SCAN_QR_PLACE（移至码板B，扫放置码）        │
│    ↓ 解析成功 → 显示完整任务码                               │
│  PICKUP_BATCH1（原料区，按拾取码抓取第一批）                  │
│    ↓ 承载检测通过                                            │
│  PLACE_ROUGH（粗加工区，按放置码映射放置第一批）              │
│    ↓                                                         │
│  TRANSFER_TO_STORAGE（暂存区，按放置码顺序转运）             │
│    ↓                                                         │
│  PICKUP_BATCH2（原料区，按拾取码抓取第二批）                  │
│    ↓ 承载检测通过                                            │
│  PLACE_ROUGH2（粗加工区，第二批放置）                        │
│    ↓                                                         │
│  STACK_STORAGE（暂存区码垛，颜色对齐下层）                   │
│    ↓                                                         │
│  RETURN_HOME（回启停区）                                     │
│    ↓                                                         │
│  DONE                                                        │
│                                                              │
│  每个状态设超时 → TIMEOUT_RECOVERY（回退重试或跳过）         │
└─────────────────────────────────────────────────────────────┘
```

---

#### 多颜色扩展（决赛新增颜色时）

若决赛引入第四、五种颜色（如黄色"4"、白色"5"），调度器的改动极小：

| 改动项 | 改动内容 | 改动量 |
|--------|----------|--------|
| `COLOR_MAP` 字典 | 添加 `"4": "YELLOW"` 等映射 | 1 行 |
| 颜色识别模块 | 用标定工具追加新颜色样本到配置文件 | 3 分钟 |
| 承载平台 | 若物料数量增加，新增槽位 | 机械改动 |
| 调度器核心逻辑 | **无需改动** | 0 行 |

这是"开闭原则"在嵌入式任务调度中的直接体现——对扩展开放，对修改关闭。

---

#### 极端场景预案：任务码含区域指定

若决赛进一步增加难度，任务码中出现指定放置区域（如粗加工区 A 位 vs B 位）：

- 示例格式：`P:213|DA:12|DB:3`（前两个放 A 区，第三个放 B 区）
- 应对：在 `place_queue` 的每条记录中新增 `"zone"` 字段，路径规划模块根据 `zone` 导航到对应区域，调度器核心逻辑不变，只扩展数据结构。

---

## 四、赛项具体要求

### 1. 初赛 — 设计文档

#### 核心要点

| 评分维度 | 正确性要求 | 创新性要求 |
|----------|------------|------------|
| 结构设计 | 传动原理与机构设计计算正确，选材和工艺合理 | 有独立见解及创新点 |
| 控制系统设计 | 电路设计正确，器件选择合理，控制流程正确 | 有独立见解及创新点 |
| 规范性 | 图纸表达完整，标注规范；文字描述准确、清晰 | — |

#### 💡 备赛建议

1. **文档与实物严格对应**：评委对照实物审阅文档，不一致处会被质疑；每次修改实物后文档必须同步更新。
2. **创新性要具体**：不要泛泛描述"采用视觉识别"，而要说明"采用 LAB 色彩空间马氏距离分类器 + 配置文件驱动，支持赛场 3 分钟快速标定新颜色"等具体技术点，体现独立见解。
3. **控制流程推荐状态机图**：比普通流程图更专业，逻辑更严密，能清晰展示各状态转换条件和异常处理路径，且与代码实现直接对应。

---

### 2. 初赛 — 现场运行流程

#### 核心要点

**准备阶段：**
- 现场抽签决定赛场、赛位号
- 调试时间结束后，机器人放置于启停区（蓝色），等待发车
- **⚠️ 必须采用"一键式"启动，机器人上必须有明确标识**

**运行流程（共 8 步）：**

```
① 机器人移至二维码板前 → 读取二维码获取任务编码
② 移至原料区 → 按任务码顺序抓取第一批物料（3个）并承载于机器人上
③ 运至粗加工区 → 按颜色放置到对应颜色区域（精度影响色环得分）
④ 按第一批搬运顺序，将粗加工区物料转运至暂存区对应颜色区域
⑤ 返回原料区 → 按任务码顺序抓取第二批物料（3个）并承载于机器人上
⑥ 运至粗加工区 → 按颜色放置到对应颜色区域
⑦ 按第二批搬运顺序，将粗加工区物料转运至暂存区 → 在第一批物料上码垛放置
   （颜色须一致，且第一批必须已正确放置）
⑧ 完成任务 → 机器人回到启停区
```

**关键限制条件：**
- **⚠️ 每次装载物料数量不超过 3 个**
- **⚠️ 物料必须承载于机器人上方可行驶向下一区域**（本区域内不受此限制）
- **⚠️ 物料未承载于机器人就驶向下一区域：该次运送不计入成绩，但时间连续计算**
- **⚠️ 码垛放置前提：颜色须一致，且下层物料必须已正确放置**
- 每队有两次运行机会，取最好成绩

**晋级规则（成绩相同时）：**  
初赛总成绩 → 现场初赛成绩 → 完成时间（少者优先）→ 组委会决定

#### 💡 备赛建议

1. **有限状态机（FSM）实现任务流程**：将 8 个步骤硬编码为状态，每个状态加超时保护，异常可回退重试，避免程序在边界情况下卡死。
2. **物料上机检测**：用重量传感器或红外传感器确认物料已承载于机器人上，再触发行进指令，彻底规避"空载驶向下一区域不计分"的风险，此传感器逻辑可模块化复用。
3. **回启停区时间预算**：在程序中设置总时间预算，提前 X 秒触发返回逻辑（X 根据实测返回时间确定），防止任务超时导致无法回到出发区失分。
4. **一键启动可靠性**：建议使用大红色硅胶按钮+文字标签，并在调试期间验证每次从冷启动都能稳定运行，不依赖调试时的临时配置状态。
5. **码垛前置条件保障**：第一批放置精度极为关键，第一批误放会导致第二批码垛无效，建议为第一批放置专门设计精度校正逻辑（如视觉微调）。

---

### 3. 决赛 — 创新实践环节

#### 核心要点

- 重点考察**设计能力与加工制作能力**
- **⚠️ 参赛队须按规定完成相关零件的设计与制作，并替换安装到作品上完成调试**
- **⚠️ 未按规定完成规定零件制作：取消比赛资格**
- **⚠️ 完成制作但未成功将新零件替换安装调试完成后续运行：扣除决赛总成绩的 50%**

**现场提供设备：**
- 220V 交流电
- 3D 打印、激光切割设备及相应材料（赛前提供设备操作培训）

**各队自备：**
- 笔记本电脑、相关软硬件、零部件、元器件、安装调试工具
- **⚠️ 有安全隐患的物品不能带入实践场地，否则取消比赛资格**

#### 💡 备赛建议

1. **参数化 CAD 模板**：提前建立参数化零件模型，关键尺寸用变量驱动，赛场仅需修改参数即可生成图纸，大幅缩短设计时间；模板本身即为可复用资产。
2. **熟悉设备操作**：赛前培训务必认真参加，提前掌握 3D 打印参数（层高/速度/填充率）和激光切割参数（功率/速度/焦距），赛场不允许反复试错。
3. **装配余量设计**：赛场快速加工精度低于实验室，配合尺寸建议比图纸标注松 0.1–0.2mm，防止因加工误差导致无法装配。
4. **应急耗材清单**：建议携带螺丝刀全套、热熔胶枪、扎带、备用螺钉及常用传感器/电机备件，赛场替换元件比重新调试更快。

---

### 4. 决赛 — 现场决赛

#### 核心要点

- 参照初赛流程，**按现场发布的决赛任务**完成物料运输任务
- **⚠️ 决赛物料形状多样，与初赛不同（含圆柱/方形/三角/球/锥/组合体等）**
- **⚠️ 决赛任务码可能采用拾取码 + 放置码分离的双码格式，颜色种类亦可能增加**
- 每队有两次运行机会，取最好成绩
- 成绩相同时：按现场决赛得分→完成时间排序（分高者优先，时间少者优先）

#### 💡 备赛建议

1. **适应性优先于针对性**：决赛任务现场公布，手爪兼容多种形状、路径规划鲁棒性强，比仅针对初赛物料优化更重要；通用设计在决赛中的价值高于专用优化。
2. **双码调度器提前就绪**：将双码任务调度器（见三（四补））在初赛结束后立即集成并测试，决赛当天收到命题后只需按格式更新解析插件，不依赖赛场现场开发。
3. **颜色识别快速重标定**：决赛若新增颜色，利用已准备好的标定脚本，在调试时间内 3 分钟完成新颜色标定，更新配置文件后立即验证识别准确率。
4. **两次机会分配**：第一次稳定完成基础任务（保障分数下限），第二次追求速度与精度（冲击高分）；切勿两次都激进导致全部失分。
5. **时间竞争意识**：成绩相同时拼完成时间，日常训练时系统记录每次完整任务时间，持续优化路径效率和各动作节拍，每减少一秒都是竞争优势。

---

## 附：快速备赛 Checklist

### 硬件层面
- [ ] 外形尺寸验证（300×300×400mm，每次结构改动后必检）
- [ ] 任务码显示装置安装并验证可见性（字高≥8mm，四向无遮挡）
- [ ] 电池容量测试（满载完整任务续航）
- [ ] 补光灯安装方向验证（垂直向下）
- [ ] 一键启动按钮标识清晰
- [ ] 承载平台定位槽/限位挡柱安装（防物料运动中滑落）

### 软件层面——初赛
- [ ] 二维码识别模块（60–120cm 范围稳定解码，支持重试逻辑）
- [ ] 颜色识别模块（HSV 或 LAB 阈值标定，含赛场光照条件验证）
- [ ] 颜色配置文件 `colors_config.json` 建立（参数化，支持快速修改）
- [ ] 任务调度器（覆盖全部 36 种初赛 `3!×3!` 任务码组合测试）
- [ ] 物料承载检测逻辑（传感器确认后再行进）
- [ ] 总时间预算 + 自动返回启停区逻辑
- [ ] 状态机异常处理（每个状态加超时保护 + 回退重试）

### 软件层面——决赛专项 🆕
- [ ] 颜色识别升级：LAB / HSV+RGB / k-NN 至少实现一种增强方案
- [ ] 颜色标定脚本开发（≤60 行，3 分钟完成一种新颜色标定）
- [ ] 置信度阈值兜底逻辑（低置信度触发重识别，不强行判断）
- [ ] 双码任务调度器集成（parse_pickup + parse_place 分离架构）
- [ ] 多格式解析插件准备（`plus` / `pipe` / `slash` / `json` 格式各备一个）
- [ ] 双二维码状态机扩展（新增"读放置码"状态）
- [ ] 全组合测试（双码模式下 `3!×3!×3!×3!` = 1296 种组合抽样测试）
- [ ] `COLOR_MAP` 预留扩展槽位（4=黄、5=白等预置但不激活）

### 文档层面
- [ ] 设计文档与实物一致（结构/电路/控制流程）
- [ ] 图纸标注规范，工艺说明清晰
- [ ] 创新点具体描述（含颜色识别方案技术细节、调度器架构说明）

### 决赛现场操作
- [ ] 参数化 CAD 模板准备完毕（可直接出图）
- [ ] 3D 打印/激光切割设备操作培训参加
- [ ] 应急耗材清单准备（螺丝刀、热熔胶、扎带、备用件）
- [ ] 决赛物料多形状适配测试（圆柱/方形/锥体等）
- [ ] 赛场调试时间内完成颜色重标定（覆盖实验室标定值）

---

> 本文档依据 2026 年广东省大学生工程实践与创新能力大赛智能物流搬运赛项命题整理，V2 版在 V1 基础上新增决赛颜色识别增强策略与双码任务调度器设计，以赛场实际公布的规则为准。
