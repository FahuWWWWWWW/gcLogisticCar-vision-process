# Vision 模块硬件选型与视觉升级分析

> 分析日期：2026-07-27
> 分析方法：逐文件审查当前 Vision 代码 + 赛题规则精读 + GitHub 同类项目对比 + 多硬件平台(树莓派/泰山派RK3566/3576/庐山派/OpenMV)评估

---

## 1. 赛题核心约束澄清

### 初赛（智能搬运）：同形状、6种颜色

- 物料形状：**一种**（赛题原图，固定形状）
- 颜色：红、黄、蓝、绿、黑、浅蓝，每色2个共12个
- 任务码：`156+123+516+231` 格式，通过二维码获取
- 转盘：直径300mm，3个物料120°均匀分布，6-10s/圈

### 决赛（智能搬运）：形状多样化

- 增加了圆柱体、方形体、三角形、球体、锥体、组合体等
- 颜色仍是红黄蓝绿黑浅蓝

### 评分精度：1环 = φ+3mm（即放置误差≤±1.5mm）

**纠错说明**：决赛的"球/锥/组合体"等复杂形体是智能搬运赛项自身的内容，非智能救援赛项。但初赛阶段仅需处理同形状物料。

---

## 2. 当前 Vision 模块完整文件清单

### 2.1 核心管线（必读，已完整实现）

| 文件 | 行数 | 职责 | 状态 |
|------|------|------|:--:|
| `main.py` | 384 | FSM 状态机主循环，整合所有视觉模块 | ✅ |
| `utils/serial_comm.py` | ~200 | JSON 串口通信，USB-CDC 自动重连 | ✅ |
| `utils/camera_stream.py` | ~120 | 多线程摄像头流（V4L2+MJPG） | ✅ |
| `utils/logger.py` | ~80 | 标准化日志 | ✅ |
| `utils/timer.py` | ~60 | 帧率计时（FPSCounter） | ✅ |
| `utils/debug_saver.py` | ~30 | DEBUG 模式中间图像保存 | ✅ |
| `utils/nori_awb.py` | ~100 | Nori 相机 ISP 白平衡锁定（ctypes V4L2） | ✅ |

### 2.2 颜色识别模块

| 文件 | 行数 | 职责 | 状态 |
|------|------|------|:--:|
| `color_detection/color_extractor.py` | 152 | LAB 空间 + CLAHE 颜色提取，含 fallback 距离投票 | ✅ |
| `color_detection/color_tuner.py` | ~250 | 实时 LAB 三通道滑动条调参 GUI | ✅ |
| `color_detection/capture_images.py` | ~30 | 图像采集工具 | ✅ |
| `color_detection/offline_image_calibrator.py` | ~80 | 离线图像标定 | ✅ |

### 2.3 圆环检测模块

| 文件 | 行数 | 职责 | 状态 |
|------|------|------|:--:|
| `shape_detection/ring_detector.py` | 233 | 拓扑层级分析 + 椭圆拟合截断圆环 + NMS | ✅ |
| `test_ring_detector.py` | ~80 | 圆环检测参数实时调优 | ✅ |

### 2.4 二维码解码模块

| 文件 | 行数 | 职责 | 状态 |
|------|------|------|:--:|
| `qrcode_barcode/qr_decoder.py` | ~60 | WeChatQRCode CNN 解码 + pyzbar 备用 | ✅ |

### 2.5 标定模块

| 文件 | 行数 | 职责 | 状态 |
|------|------|------|:--:|
| `calibration/camera_calibrator.py` | ~300 | 张正友棋盘格内参标定 + remap 查表极速去畸变 | ✅ |
| `calibration/perspective_calibrator.py` | ~250 | 四点鼠标交互外参透视变换（像素→mm） | ✅ |
| `calibration/detect_checkerboard_size.py` | ~50 | 自动检测棋盘格格子数 | ✅ |

### 2.6 OCR 模块

| 文件 | 行数 | 职责 | 状态 |
|------|------|------|:--:|
| `ocr_recognition/ocr_engine.py` | ~100 | EasyOCR / Tesseract 双后端数字识别 | ✅ |

### 2.7 测试与调试

| 文件 | 行数 | 职责 | 状态 |
|------|------|------|:--:|
| `tests/integration_benchmark.py` | ~150 | 端到端性能基准测试 | ✅ |
| `tests/headless_verify.py` | ~80 | 无头模式功能验证 | ✅ |
| `tests/test_serial_link.py` | ~50 | 串口链路测试 | ✅ |
| `json_test.py` | ~40 | JSON 协议打桩测试 | ✅ |

### 2.8 部署与运维

| 文件 | 职责 |
|------|------|
| `scripts/setup_ssh.py` | SSH 免密登录配置 |
| `scripts/ssh_test.py` | SSH 连接测试 |
| `verify_pi.py` | 树莓派环境验证入口 |
| `connect_pi.py` | 树莓派自动发现与连接 |
| `scan_ssh.py` | SSH 局域网扫描 |
| `read_com26.ps1` | Windows COM 口读取脚本 |
| `requirements.txt` | PC 端依赖 |
| `requirements-raspi.txt` | 树莓派端依赖 |

### 2.9 Nori 相机资料（外设适配）

| 文件 | 职责 |
|------|------|
| `Nori_public_dump.py` | Nori 公开 API 测试 |
| `Nori_Xvision_API_dump.py` | Nori 企业 SDK 测试 |
| `device_whitebalance_gain_control.cpp` | C++ 白平衡增益控制 |
| `nor_public_dump.txt` | Nori 命令参考文本 |

### 2.10 配置与模型文件

| 文件 | 内容 |
|------|------|
| `config/serial_config.yaml` | 串口配置（端口/波特率） |
| `config/color_config.yaml` | LAB 颜色阈值（red/yellow/blue/green/black/light_blue） |
| `config/camera_params.npz` | 相机内参+畸变系数 |
| `config/perspective_params.npz` | 透视变换矩阵 |
| `models/wechat_qrcode/*` | WeChatQRCode 4个 Caffe 模型文件 |

### 2.11 文档

| 文件 | 内容 |
|------|------|
| `README.md` | 项目主说明 |
| `docs/树莓派部署与运行指南.md` | 部署流程 |
| `docs/项目开发计划.md` | 子模块开发计划 |
| `docs/相机标定开发与测试指南.md` | 标定流程与 FAQ |
| `docs/上下位机串口通信与测试指南.md` | 串口调试指南 |
| `calibration/README.md` | 标定模块说明 |
| `智能物流搬运赛项_备赛指南.md` | 赛题规则摘要 |
| `content_of_competition/2027智能+命题.pdf` | 赛题原文 |
| `content_of_competition/2026智能+命题.pdf` | 上届赛题（对比参考） |

### 2.12 空壳模块（`__init__.py` only，待开发）

| 目录 | 说明 |
|------|------|
| `object_detection/` | 目标检测（预留，空壳） |
| `pose_estimation/` | 位姿估计（预留，空壳） |

---

## 3. 硬件选型深度对比

### 3.1 候选平台一览

| 维度 | 树莓派5 (当前) | 泰山派RK3566 | 泰山派RK3576 | OpenMV | 庐山派 |
|------|:--:|:--:|:--:|:--:|:--:|
| CPU | 4×A76@2.4GHz | 4×A55@2.0GHz | 4×A76+4×A55 | STM32H7@480MHz | 未知 |
| NPU | 无 | 1 TOPS | 6 TOPS | 无 | 未知 |
| GPU | VideoCore VII | Mali-G52 | Mali-G610 | 无 | 未知 |
| RAM | 8GB LPDDR4X | 4GB LPDDR4 | 8GB LPDDR5 | <1MB SRAM | 未知 |
| OpenCV | ✅ 完整支持 | ✅ 完整支持 | ⚠️ 包可能缺失 | ❌ 不支持 | 未知 |
| YOLO推理 | 慢(软件) | RKNN加速(快) | RKNN加速(极快) | ❌ | 未知 |
| GPIO | 40Pin 树莓派标准 | 40Pin 兼容 | 40Pin 兼容 | 有限IO | 未知 |
| CSI摄像头 | 2×MIPI | 1×MIPI | 1×MIPI | OV系列自带 | 未知 |
| 功耗 | 12-15W | 5-8W | 8-12W | <1W | 未知 |
| 价格 | ~500元 | ~400元 | ~800元+ | ~400元 | 未知 |
| 社区/文档 | ★★★★★ | ★★★★ | ★★★ | ★★★★ | ★ |
| 工创赛使用 | 大量项目 | 有(广东赛区) | 无 | 辅助摄像头 | 无 |

### 3.2 工创赛 GitHub 同类项目对比

| 项目 | 成绩 | 主控 | 视觉方案 | 与你项目相似度 |
|------|:--:|------|------|:--:|
| [gcs-gold-medal](https://github.com/yuanxiexie531-glitch/gcs-gold-medal) | 国金 | STM32+PC | Python OpenCV LAB/HSV | ★★★★★ |
| [GX-Intelligent-Logistics-Silver](https://github.com/liaojingwu20041031/GX-Intelligent-Logistics-Silver) | 国银 | 树莓派+STM32F103 | OpenCV HSV+ArUco | ★★★★★ |
| [Engineering-innovation-competition-vision](https://github.com/Blue-Net-Team/Engineering-innovation-competition-vision) | 广东赛区 | **泰山派RK3566** | HSV色相重构+圆环定位 | ★★★★ |
| [DeliveryCarVision](https://github.com/dixiatielu/DeliveryCarVision) | — | 树莓派5 | YOLO+NCNN+ROS2 | ★★★ |

### 3.3 结论：不需要换主控，不需要加协处理器

**国金和国银项目用的就是树莓派+传统OpenCV，没有NPU协处理器。**

不需要协处理器的核心理由：

1. **LAB 阈值法足够** — 6 种颜色（红黄蓝绿黑浅蓝）在 LAB 空间自然可分，不需要深度学习做分类
2. **转盘不需要卡尔曼滤波** — 3 个物料 120° 固定分布，摄像头架高俯瞰即可同时识别 3 色的顺序和位置
3. **电源压力** — 泰山派额外 5V/3A ≈ 15W，整机功耗破 40W，比赛不允换电池
4. **通信复杂度** — 树莓派↔泰山派↔STM32 三节点链路增加故障点
5. **OpenMV** — 不能做为主控，不能跑 OpenCV，但可以作为底部辅助摄像头做近距离 IBVS 对准
6. **庐山派** — 全网无工创赛项目，资料太少，直接放弃
7. **泰山派 RK3576** — 性能过剩下放不确定，驱动不稳（太新），不推荐

---

## 4. LAB 颜色识别能力分析

### 4.1 6 色在 LAB 空间的可分性

```
LAB 空间设计原理：
   L 通道 = 亮度（0黑~100白），与颜色无关
   A 通道 = 红(+) ↔ 绿(-)，纯线性
   B 通道 = 黄(+) ↔ 蓝(-)，纯线性

各颜色在 A/B 通道的分布：
  红色：  A>>0, B≈0        → 极易区分，与所有颜色都隔得远
  绿色：  A<<0, B≈0        → 极易区分
  黄色：  A≈0, B>>0        → 极易区分
  蓝色：  A≈0, B<<0        → 极易区分
  黑色：  L<<100           → 亮度最低，辅助 A/B 即可区分
  浅蓝：  A≈0, B<0, L偏高  → 与蓝色在 B 通道上重叠，需要 L 通道辅判
```

**唯一需要精细调参的组合：浅蓝 vs 蓝色。** 其余 5 种颜色在 LAB 空间各占一个象限，天然不冲突。

### 4.2 当前 LAB 方案已具备的光照鲁棒性

你的 `color_extractor.py` 三层抗光照机制：

1. **CLAHE 自适应直方图均衡化** — 消除阴影/高光对 L 通道的影响
2. **形态学开闭运算** — 去除离散噪点、填补色块内部空洞
3. **fallback 距离投票** — inRange 失效时自动切换到 LAB 欧氏距离软投票（`extract_robust`）

这套方案在 2025 国金项目同类代码中也采用，已被验证有效。

### 4.3 转盘俯瞰方案（不需要角速度预测）

```
摄像头安装高度 ≥ 转盘直径 → 可同时看到 3 个物料

流程：
  1. 摄像头俯视 → 一次采集画面内 3 个物料
  2. LAB 颜色识别 → 输出 [{color: "red", pos: (x1,y1)}, ...]
  3. 任务码已知顺序（如 1-5-6）→ 等待目标颜色转到机械臂正前方
  4. 视觉判断角度 → 下发抓取指令 → 抓取

不需要卡尔曼滤波。不需要预测角速度。只需要颜色判别。
```

---

## 5. 真正需要做的升级（按优先级排序）

### 5.1 P0-致命：先止血（与视觉无直接关系，但必须先修）

| 问题 | 当前状态 | 方案 | 涉及文件 |
|------|------|------|----------|
| 光流计不可靠 | LC-302 严重漂移 | 换正交编码轮 | `PLAN/P1-01` |
| 速度轨迹规划缺失 | P控制急停急启 | 梯形/S曲线规划 | `PLAN/P2-01` |
| 看门狗空桩 | T9_Watchdog 空循环 | IWDG + 任务心跳 | `PLAN/P7-01` |
| CRUISE_TO 未闭环 | CMD 0x20 仅打印日志 | 接入 PositionController | `T7_Path_Exec.c` |

### 5.2 P1-重大：视觉需要补的能力

| 问题 | 当前状态 | 方案 | 涉及文件 |
|------|------|------|----------|
| 放置精度不够 | 像素偏移模式(开环) | **完整 IBVS 视觉伺服闭环** | `Vision/main.py` |
| 转盘俯瞰未验证 | 代码未启动 | 摄像头架高→3色同时识别 | `Vision/main.py` + 结构 |
| 浅蓝/蓝易混淆 | LAB 阈值不够精细 | B通道置信度+多帧投票窗口 | `color_config.yaml` |
| 光照突变 | CLAHE 有限 | 运行时白平衡归一化 | `Vision/main.py` |
| 圆环截断稳定性 | 单帧检测 | 多帧融合+圆心投票 | `ring_detector.py` |

### 5.3 P2-提升：锦上添花

| 问题 | 方案 |
|------|------|
| OpenMV 底部辅助 | 近距离 IBVS 精对准（10-20cm 工作距离） |
| 仿真环境 | Webots 搭建麦轮底盘+场地模型 |
| 自动化标定 | 一键脚本跑完整标定流程 |

---

## 6. 代码质量评价

### 优点

- **FSM 状态机设计合理**：事件驱动、任务队列排空取最新、超时保护
- **LAB 颜色方案先进**：比 HSV 更适合光照变化的工业环境
- **圆环检测巧妙**：拓扑层级分析 + 椭圆拟合截断圆环 + NMS，算法设计有工程深度
- **标定管线完整**：内参(remap查表) + 外参(透视变换) 双标定
- **串口通信健壮**：断线自动重连、回调注册、帧过滤防阻塞

### 需要改进

- **IBVS 闭环未实现**：当前仅有像素偏移模式，缺少图像→运动的全闭环
- **转盘场景未验证**：代码中无转盘多物料同时识别的逻辑
- **光照 fallback 未实战测试**：`extract_robust` 的距离投票法在比赛场地未验证
- **无运行时自适应**：LAB 阈值是写死的，无法在比赛中自动补偿灯光变化

---

## 7. 最终结论

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   ✦ 不需要换主控 — 树莓派5 足够，国金/国银都这样用             │
│   ✦ 不需要协处理器 — LAB 阈值法能覆盖 6 色 + 不需要YOLO深度学习 │
│   ✦ 不需要卡尔曼滤波 — 转盘 3 物料固定位置，摄像头架高俯瞰即可  │
│   ✦ 不需要大篇幅重写 — FSM/串口/标定/圆环检测代码质量高         │
│                                                                 │
│   ★ 真正该做的 —                                               │
│     1. 修完 P0 致命项（编码轮+速度规划+看门狗+CRUISE_TO）       │
│     2. 加 IBVS 放置闭环（纯软件改动 ~200行）                    │
│     3. 摄像头架高验证转盘俯瞰方案（结构改动）                   │
│     4. 浅蓝/蓝色 LAB 阈值精细标定                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```
